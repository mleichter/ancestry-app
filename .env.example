# ── Required ────────────────────────────────────────────────────────────────
DB_PASSWORD=changeme

# ── Authentication (optional — app runs open if unset) ───────────────────────
# AUTH_PASSWORD=your_login_password
# AUTH_SECRET_KEY=your_jwt_signing_secret   # generate: openssl rand -hex 32
# API_KEY=your_static_bearer_token          # for API/MCP access

# ── AI document extraction (optional) ────────────────────────────────────────
# OPENAI_API_KEY=sk-...
# OPENAI_BASE_URL=http://your-litellm:4000  # omit to use OpenAI directly
# OPENAI_MODEL=gpt-4o                       # default
