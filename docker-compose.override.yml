services:
  backend:
    image: ancestry-app-backend:dev
    build:
      context: ./backend
      target: dev
    command: uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
    environment:
      OPENAI_API_KEY: "sk-bzfasUbS_iiFKzZ2msuVYA"       # set your LiteLLM key here
      OPENAI_BASE_URL: "http://192.168.178.20:4000"      # e.g. http://litellm:4000
      OPENAI_MODEL: "gpt-4o"  # e.g. claude-sonnet-4-6
      AUTH_PASSWORD: "${AUTH_PASSWORD}"
      AUTH_SECRET_KEY: "${AUTH_SECRET_KEY}"
      API_KEY: "${API_KEY}"
    depends_on:
      db:
        condition: service_healthy

  frontend:
    image: ancestry-app-frontend:dev
    build:
      context: ./frontend
      target: dev
    command: npm run dev -- --host 0.0.0.0
    environment:
      VITE_API_URL: http://backend:8000
    volumes:
      - ./frontend:/app
      - /app/node_modules
